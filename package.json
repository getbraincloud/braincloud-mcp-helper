{
  "name": "braincloud-mcp-helper-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Local helper MCP for brainCloud cloud-code sync, plus the shared on-disk format/sync library.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "license": "UNLICENSED"
}
